import React from 'react';
import classnames from 'classnames';
import Layout from '@theme/Layout';
import Link from '@docusaurus/Link';
import useDocusaurusContext from '@docusaurus/useDocusaurusContext';
import useBaseUrl from '@docusaurus/useBaseUrl';
import styles from './styles.module.css';

const features = [
  {
    title: <>Easy to Use</>,
    imageUrl: 'img/DID_Easytouse_Illustration.svg',
    description: (
      <>
       Defined.id is a flexible open-source identity solution that can easily be implemented by developers and businesses in any project that requires the identity of people, things or devices.
      </>
    ),
  },
  {
    title: <>Full Control</>,
    imageUrl: 'img/DID_Fullcontroll_Illustration.svg',
    description: (
      <>
       This blockchain-based identity solution enables everyone to fully control their digital identity and to consciously decide which information to share and which not to share.
      </>
    ),
  },
  {
    title: <>Unique Privacy</>,
    imageUrl: 'img/DID_Uniqueprivacy_Illustration.svg',
    description: (
      <>
        Defined.Id is integrated with the end-to-end encrypted Cloud from Geens NPO. Users & businesses can backup their claims and credentials ensuring privacy and the right to be forgotten.
      </>
    ),
  },
];

function Feature({imageUrl, title, description}) {
  const imgUrl = useBaseUrl(imageUrl);
  return (
    <div className={classnames('col col--4', styles.feature)}>
      {imgUrl && (
        <div className="text--center">
          <img className={styles.featureImage} src={imgUrl} alt={title} />
        </div>
      )}
      <h3>{title}</h3>
      <p>{description}</p>
    </div>
  );
}

function Home() {
  const context = useDocusaurusContext();
  const {siteConfig = {}} = context;
  return (
    <Layout
      title={`Hello from ${siteConfig.title}`}
      description="Description will go into a meta tag in <head />">
      <header className={classnames('hero hero--primary', styles.heroBanner)}>
        <div className="container">
          <h1 className="hero__title">{siteConfig.title}</h1>
          <p className="hero__subtitle">{siteConfig.tagline}</p>
          <div className={styles.buttons}>
            <Link
              className={classnames(
                'button button--outline button--secondary button--lg',
                styles.getStarted,
              )}
              to={useBaseUrl('docs/working-with-dids')}>
              Get Started
            </Link>
          </div>
        </div>
      </header>
      <main>
        {features && features.length && (
          <section className={styles.features}>
            <div className="container">
              <div className="row">
                {features.map((props, idx) => (
                  <Feature key={idx} {...props} />
                ))}
              </div>
            </div>
          </section>
        )}
      </main>
    </Layout>
  );
}

export default Home;
